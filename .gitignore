# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Environments
.venv/
venv/
.env

# Training artifacts / data (never commit — large or non-redistributable)
checkpoints/
logs/
run_logs/
manifests/
exported_vocoders/
mfa_corpus/
mfa_aligned/
mfa_tmp*/
ttscond_mfa_*/
# Exclude the bulk eval outputs, but ship the four aggregate tone reports behind
# Figure 7 (confusion matrices only -- no audio, no voice names, ~4 KB each).
eval_reports/*
!eval_reports/tone_val_no_overlap_*_20k.json
eval_samples/
listening_test/
human_eval_*/
zipvoice_*/
*.wav
*.flac
*.mp3
*.pt
*.ckpt
*.tar
*.zip

# Editor / OS
.DS_Store
.idea/
.vscode/
.gradio/
.claude/
